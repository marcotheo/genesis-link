# ARK-POINT COGNITO SIGN UP CUSTOM MESSAGE FUNCTION

### LOCAL & DEPLOYMENT GUIDE

1. Makefile has 2 commands `Make build` and `Make root-build`
2. no need to run any of these manually unless there is a reason
3. this command `Make root-build` will be run by sst
   - refer to the following package.json scripts which are `prod`, `preview`, and `dev`
   - which already gonna build this function by running the make command
